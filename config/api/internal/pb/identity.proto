syntax = "proto3";
package dogma.config.api.v1;

option go_package = "github.com/dogmatiq/enginekit/config/api/internal/pb";

// Identity is a protocol buffers representation of the identity.Identity type.
message Identity {
  string name = 1;
  string key = 2;
}

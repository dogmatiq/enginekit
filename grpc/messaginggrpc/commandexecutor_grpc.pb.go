// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             v6.33.1
// source: github.com/dogmatiq/enginekit/grpc/messaginggrpc/commandexecutor.proto

package messaginggrpc

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	CommandExecutorAPI_ExecuteCommand_FullMethodName = "/dogma.messaging.v1.CommandExecutorAPI/ExecuteCommand"
)

// CommandExecutorAPIClient is the client API for CommandExecutorAPI service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// CommandExecutorAPI is a service for executing commands within a Dogma
// application hosted by the engine on the server.
//
// It is the remote analogue of the [dogma.CommandExecutor] interface.
type CommandExecutorAPIClient interface {
	// ExecuteCommand submits a command for execution, and optionally observes
	// the events that are recorded as a result of executing it.
	//
	// If the requested application key is unknown to the server it MUST return
	// a NOT_FOUND error with an attached [UnrecognizedApplication] value. See
	// [UnrecognizedApplicationError].
	//
	// If the requested command type ID does not identify a command that is
	// executable within the target application the server MUST return an
	// INVALID_ARGUMENT error with an attached [UnrecognizedCommandType] value.
	// See [UnrecognizedCommandTypeError].
	//
	// If the command data cannot be unmarshaled the server MUST return an
	// INVALID_ARGUMENT error with an attached [MalformedCommand] value. See
	// [MalformedCommandError].
	//
	// If the command is well-formed but fails the application's validation
	// logic the server MUST return an INVALID_ARGUMENT error with an attached
	// [InvalidCommand] value. See [InvalidCommandError].
	//
	// The requested event type IDs MUST identify event types that may be
	// recorded by the target application. If any other type IDs are requested
	// the server MUST return an INVALID_ARGUMENT error with an attached
	// [UnrecognizedEventType] value for each unrecognized type ID. See
	// [UnrecognizedEventTypeError].
	//
	// The first response MUST be a [CommandAccepted] operation, sent once the
	// engine has taken ownership of the command. Ownership means that the
	// command has been durably accepted for execution; it does not mean that
	// handling of the command is complete.
	//
	// Subsequent responses each deliver a single event that was recorded as a
	// result of executing the command and that matches one of the requested
	// event type IDs. If no event type IDs are requested the server MUST end
	// the stream immediately after sending the [CommandAccepted] operation.
	//
	// The server MUST end the stream with an OK status once it determines that
	// no further relevant events can occur. Any other outcome, such as a
	// network failure or server shutdown, leaves the client unable to resume
	// observation; the client may re-execute the command using the same
	// idempotency key to confirm its acceptance.
	//
	// If the command is a duplicate, as identified by its idempotency key, the
	// server MUST NOT execute it again, but MUST still send a [CommandAccepted]
	// operation. The server SHOULD deliver the relevant events recorded by the
	// original execution, but MAY end the stream immediately if it is unable to
	// determine which events those are.
	ExecuteCommand(ctx context.Context, in *ExecuteCommandRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ExecuteCommandResponse], error)
}

type commandExecutorAPIClient struct {
	cc grpc.ClientConnInterface
}

func NewCommandExecutorAPIClient(cc grpc.ClientConnInterface) CommandExecutorAPIClient {
	return &commandExecutorAPIClient{cc}
}

func (c *commandExecutorAPIClient) ExecuteCommand(ctx context.Context, in *ExecuteCommandRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ExecuteCommandResponse], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &CommandExecutorAPI_ServiceDesc.Streams[0], CommandExecutorAPI_ExecuteCommand_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[ExecuteCommandRequest, ExecuteCommandResponse]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type CommandExecutorAPI_ExecuteCommandClient = grpc.ServerStreamingClient[ExecuteCommandResponse]

// CommandExecutorAPIServer is the server API for CommandExecutorAPI service.
// All implementations should embed UnimplementedCommandExecutorAPIServer
// for forward compatibility.
//
// CommandExecutorAPI is a service for executing commands within a Dogma
// application hosted by the engine on the server.
//
// It is the remote analogue of the [dogma.CommandExecutor] interface.
type CommandExecutorAPIServer interface {
	// ExecuteCommand submits a command for execution, and optionally observes
	// the events that are recorded as a result of executing it.
	//
	// If the requested application key is unknown to the server it MUST return
	// a NOT_FOUND error with an attached [UnrecognizedApplication] value. See
	// [UnrecognizedApplicationError].
	//
	// If the requested command type ID does not identify a command that is
	// executable within the target application the server MUST return an
	// INVALID_ARGUMENT error with an attached [UnrecognizedCommandType] value.
	// See [UnrecognizedCommandTypeError].
	//
	// If the command data cannot be unmarshaled the server MUST return an
	// INVALID_ARGUMENT error with an attached [MalformedCommand] value. See
	// [MalformedCommandError].
	//
	// If the command is well-formed but fails the application's validation
	// logic the server MUST return an INVALID_ARGUMENT error with an attached
	// [InvalidCommand] value. See [InvalidCommandError].
	//
	// The requested event type IDs MUST identify event types that may be
	// recorded by the target application. If any other type IDs are requested
	// the server MUST return an INVALID_ARGUMENT error with an attached
	// [UnrecognizedEventType] value for each unrecognized type ID. See
	// [UnrecognizedEventTypeError].
	//
	// The first response MUST be a [CommandAccepted] operation, sent once the
	// engine has taken ownership of the command. Ownership means that the
	// command has been durably accepted for execution; it does not mean that
	// handling of the command is complete.
	//
	// Subsequent responses each deliver a single event that was recorded as a
	// result of executing the command and that matches one of the requested
	// event type IDs. If no event type IDs are requested the server MUST end
	// the stream immediately after sending the [CommandAccepted] operation.
	//
	// The server MUST end the stream with an OK status once it determines that
	// no further relevant events can occur. Any other outcome, such as a
	// network failure or server shutdown, leaves the client unable to resume
	// observation; the client may re-execute the command using the same
	// idempotency key to confirm its acceptance.
	//
	// If the command is a duplicate, as identified by its idempotency key, the
	// server MUST NOT execute it again, but MUST still send a [CommandAccepted]
	// operation. The server SHOULD deliver the relevant events recorded by the
	// original execution, but MAY end the stream immediately if it is unable to
	// determine which events those are.
	ExecuteCommand(*ExecuteCommandRequest, grpc.ServerStreamingServer[ExecuteCommandResponse]) error
}

// UnimplementedCommandExecutorAPIServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedCommandExecutorAPIServer struct{}

func (UnimplementedCommandExecutorAPIServer) ExecuteCommand(*ExecuteCommandRequest, grpc.ServerStreamingServer[ExecuteCommandResponse]) error {
	return status.Error(codes.Unimplemented, "method ExecuteCommand not implemented")
}
func (UnimplementedCommandExecutorAPIServer) testEmbeddedByValue() {}

// UnsafeCommandExecutorAPIServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to CommandExecutorAPIServer will
// result in compilation errors.
type UnsafeCommandExecutorAPIServer interface {
	mustEmbedUnimplementedCommandExecutorAPIServer()
}

func RegisterCommandExecutorAPIServer(s grpc.ServiceRegistrar, srv CommandExecutorAPIServer) {
	// If the following call panics, it indicates UnimplementedCommandExecutorAPIServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&CommandExecutorAPI_ServiceDesc, srv)
}

func _CommandExecutorAPI_ExecuteCommand_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(ExecuteCommandRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(CommandExecutorAPIServer).ExecuteCommand(m, &grpc.GenericServerStream[ExecuteCommandRequest, ExecuteCommandResponse]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type CommandExecutorAPI_ExecuteCommandServer = grpc.ServerStreamingServer[ExecuteCommandResponse]

// CommandExecutorAPI_ServiceDesc is the grpc.ServiceDesc for CommandExecutorAPI service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var CommandExecutorAPI_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "dogma.messaging.v1.CommandExecutorAPI",
	HandlerType: (*CommandExecutorAPIServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "ExecuteCommand",
			Handler:       _CommandExecutorAPI_ExecuteCommand_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "github.com/dogmatiq/enginekit/grpc/messaginggrpc/commandexecutor.proto",
}
